<%= form_for @libro, :html => { :class => "form-horizontal libro" } do |f| %>

  <% if @libro.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@libro.errors.count, "error") %> prohibited this libro from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @libro.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>
     <%=  f.hidden_field :user_id , :value => current_user.id
    %>
  <div class="form-group">
    <%= f.label :nombre, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :nombre, :class => 'form-control' %>
    </div>
    <%=f.error_span(:nombre) %>
  </div>
  <div class="form-group">
    <%= f.label :precio, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :precio, :class => 'form-control' %>
    </div>
    <%=f.error_span(:precio) %>
  </div>
  <div class="form-group">

    <%= f.label :categoria, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
       <%= f.select(:categoria, options_for_select(["Acción", "Novela", "Ficcion", "Comedia"]), {:class => 'form-control', :readonly =>'readonly',:name =>'libro[categoria]'}) %>
    </div>
    <%=f.error_span(:categoria) %>
  </div>
  <div class="form-group">
    <%= f.label :formato, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
           <%= f.select(:formato, options_for_select(["Impreso", "Fisico","Digital"]), {:class => 'form-control', :readonly =>'readonly', :name =>'libro[formato]'}) 
           %>
    </div>
    <%=f.error_span(:formato) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                libros_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
